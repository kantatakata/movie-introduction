<div class='container px-5 px-sm-0'>
  <%= render 'layouts/errors', obj: @theater %>
  <div class='row'>
    <div class='col-md-2'>
      <!--会員情報-->
      <%= render 'public/users/info', user: @user %>
      <!--映画新規投稿-->
      <%= render 'public/theaters/form', theater: @theater %>
    </div>
    <div class='col-md-9 offset-md-1 mt-4'>
      <h3><%= @user.name %>さんのおすすめ作品一覧</h3>
      <!--映画投稿一覧-->
      <%= render 'public/theaters/index', theaters: @theaters %>
    </div>
  </div>
</div>
